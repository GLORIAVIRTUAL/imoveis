/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any specific configurations here if needed in the future
  // For now, a basic config might be enough
  reactStrictMode: true, // Example configuration
};

export default nextConfig;

